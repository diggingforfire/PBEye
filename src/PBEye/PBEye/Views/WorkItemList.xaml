<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:PBEye.Converters;assembly=PBEye"
             x:Class="PBEye.Views.WorkItemList"
						 Title="Work items"
						 x:Name="WorkItemList"
						 >
	<ContentPage.Resources>
		<ResourceDictionary>
			<converters:WorkItemTypeToColorConverter x:Key="WorkItemTypeToColorConverter" />
		</ResourceDictionary>
	
	</ContentPage.Resources>
	
	<ListView ItemsSource="{Binding WorkItems, Source={x:Reference WorkItemList}}" HasUnevenRows="True">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<StackLayout 
						Orientation="Horizontal">
						
							<StackLayout BackgroundColor="{Binding Type, Converter={StaticResource WorkItemTypeToColorConverter }}" WidthRequest="10"></StackLayout>
						<StackLayout Orientation="Horizontal" HorizontalOptions="Start" Padding="20, 10, 20, 10">

							<Label Text="{Binding WorkItemNumberAndTitle}" TextColor="Black" />
						</StackLayout>
						
					</StackLayout>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>